<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity LeaveEntitlement.
    -->
    <changeSet id="20210430144953-1" author="jhipster">
        <createTable tableName="leave_entitlement" remarks="An entitlement represents an instance when leave days are assigned to zero, one or more Staff members.\nEntitlements are typically assigned at the start of an entitlement period, such as the first day of the month.\nAlso see {@link EntitlementValue} for how the days are assigned to the {@link Staff} members.">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="entitlement_date" type="date" remarks="The date the entitlement is applied to the Staff member.">
                <constraints nullable="false" />
            </column>
            <column name="days" type="decimal(21,2)" remarks="@depricated Use the EntitlementValue">
                <constraints nullable="false" />
            </column>
            <column name="leave_type_id" type="bigint">
                <constraints nullable="false" />
            </column>
            <column name="staff_id" type="bigint">
                <constraints nullable="false" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20210430144953-1-data" author="jhipster" context="faker">
        <loadData
                  file="config/liquibase/fake-data/leave_entitlement.csv"
                  separator=";"
                  tableName="leave_entitlement">
            <column name="id" type="numeric"/>
            <column name="entitlement_date" type="date"/>
            <column name="days" type="numeric"/>
            <column name="leave_type_id" type="numeric"/>
            <column name="staff_id" type="numeric"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
